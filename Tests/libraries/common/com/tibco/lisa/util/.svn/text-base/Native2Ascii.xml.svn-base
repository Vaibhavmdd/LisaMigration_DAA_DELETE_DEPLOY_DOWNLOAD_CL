<project name="convertproject" default="convert" basedir=".">
	<target name="convert">
		<native2ascii reverse="${reverse}" encoding="${encoding}" src="${inputDir}" dest="${outputDir}" includes="${inputFile}" ext=".tmp" implementation="sun"/>
	</target>
</project>