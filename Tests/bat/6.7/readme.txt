If change input value in data.txt(data_g11n.txt), also need to change in BAT_6.7_input.rvs(BAT_6.7_input_g11n.rvs) accordingly.

For example:

If change InputCustomerID from "TIBCO" to "CDC", also need to change 
<field name="CustomerNumber" type="STRING">TIBCO</field> to 
<field name="CustomerNumber" type="STRING">CDC</field>